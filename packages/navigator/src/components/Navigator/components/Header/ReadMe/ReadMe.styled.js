import styled from '@emotion/styled';
import { Button } from '@mui/material';


export const ReadMeButton = styled(Button)({
    height: "38px",
    width: "137px",
    background: "#0F4C91",
    borderRadius: "5px",
    color: "#FFF",
    fontFamily: "Lato",
    fontSize: "16px",
    lineHeight: "22px",
});

export const ReadMeButtonIcon = styled('img')({
    height: '20px',
    width: '20px',
});
