{
  "name": "@bento-core/create-bento-app",
  "version": "1.0.0",
  "description": "This Script generate boilerplate code form Bento Reference Implementation",
  "main": "dist/index.js",
  "scripts": {
    "build": "cross-env-shell rm -rf dist && NODE_ENV=production BABEL_ENV=es babel src --out-dir dist --copy-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/CBIIT/bento-frontend",
  "publishConfig": {
    "access": "public"
  },
  "author": "CTOS Bento Team",
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.5.10",
    "axios": "^1.4.0",
    "prompts": "^2.4.2"
  },
  "bin": {
    "create-bento-app": "./bin/index.js"
  }
}
