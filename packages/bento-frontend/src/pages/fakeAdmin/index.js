import React from 'react';

// eslint-disable-next-line no-unused-vars
const accessRequestController = ({ match }) => (
  <div>
    This is Fake Admin view.
  </div>
);

export default accessRequestController;
